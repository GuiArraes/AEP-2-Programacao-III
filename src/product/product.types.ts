export interface ProductType {
    nome: String,
    quantidade: Number,
    valor: Number
}

/**
 * Fazendo um paralelo com as aulas do Henrique em Java utilizando o Spring
 * 
 * O Types é parecido com o entity / domain lá nas aulas do Henrique
 * 
 * Mas diferente do Java, aqui não estamos usando os modificadores de acessos (private, public, protected)
 * 
 * Com isso, não temos a necessidade de ter os get e set dos atributos
 * 
 */